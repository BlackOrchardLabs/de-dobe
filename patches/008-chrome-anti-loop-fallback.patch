--- a/content.js
+++ b/content.js
@@ -25,6 +25,12 @@ chrome.runtime.onMessage.addListener((msg, sender, sendResponse) => {
     return true;
   }
 });
+
+// chrome anti-loop fallback (5-second brute poll if observer fails)
+setInterval(() => {
+  if (document.querySelectorAll('[data-dedobe-id]').length === 0) rebuildSelectors();
+}, 5000);
