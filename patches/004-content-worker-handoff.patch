--- a/src/content/content.js
+++ b/src/content/content.js
@@ -40,7 +50,10 @@
     const html = wrap.innerHTML;
-    const markdown = turndown(html);   // old inline call
+    const { markdown } = await chrome.runtime.sendMessage({
+      type: 'convert',
+      html
+    });
     await storemarkdown(markdown);
   }
