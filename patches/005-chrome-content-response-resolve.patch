--- a/content.js
+++ b/content.js
@@ -15,6 +15,12 @@ chrome.runtime.onMessage.addListener((msg, sender, sendResponse) => {
   if (msg.action === 'getData') {
     const blob = buildExportBlob(); // your existing builder
+    sendResponse(blob); // resolves dangling promise
+    return true; // chrome requires explicit async response
   }
 });
